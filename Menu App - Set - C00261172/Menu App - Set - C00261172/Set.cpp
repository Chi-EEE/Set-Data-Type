#include "Set.h"

void Set::add(std::string key, std::string entry) {
	keys[count] = key;
	entries[count] = key;
}

void Set::contains(std::string key, std::string entry) {
	for (short i = 0; i < count; i++) {

	}
	keys[count] = key;
	entries[count] = key;
}